---
services:
  stack_test:
    image: postgres:18.0
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      start_period: 30s
    environment:
      POSTGRES_PASSWORD: example
